{
    "name": "copper-layouts",
    "version": "1.2.0",
    "private": true,
    "description": "A configurable Minecraft storage planner with draggable chests and editable floors.",
    "scripts": {
        "build": "node scripts/build.mjs",
        "test": "node --test tests/*.test.cjs",
        "update:items": "node scripts/update-items.mjs"
    },
    "engines": {
        "node": ">=20"
    }
}
